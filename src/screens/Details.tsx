/** @format */

import React, { FC } from "react";
import { SafeAreaView } from "react-native";

const Details: FC = () => {
	return <SafeAreaView testID="details"></SafeAreaView>;
};

export default Details;
